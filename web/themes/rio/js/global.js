/**
 * @file
 * Rio behaviors.
 */
(function (Drupal, once) {

  'use strict';

  Drupal.behaviors.rio = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal, once));
